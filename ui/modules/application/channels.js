import { Channels } from 'mvc-framework/source/MVC'

window.Application = window.Application || {}
window.Application.Channels = window.Application.Channels || new Channels()

return window.application.Channels